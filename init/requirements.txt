# Core runtime dependencies (CPU). Prefer `pip install -e .[ml,ui,dev]`
# from the repository root; this file is kept for pip-only workflows.
numpy>=1.26
opencv-contrib-python-headless>=4.9
redis>=5.0
av>=12.0

# Machine learning (the `ml` extra)
tensorflow>=2.16
scikit-learn>=1.4
scikit-image>=0.22
pandas>=2.1
pillow>=10.0
keras-vggface>=0.6

# GUI (the `ui` extra)
PySide6>=6.6

# Development (the `dev` extra)
pytest>=8.0
fakeredis>=2.21
