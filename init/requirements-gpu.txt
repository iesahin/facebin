# GPU runtime dependencies. Same as requirements.txt but with the CUDA
# build of TensorFlow. Requires a CUDA-capable GPU and matching drivers;
# see https://www.tensorflow.org/install/pip for the compatibility table.
numpy>=1.26
opencv-contrib-python-headless>=4.9
redis>=5.0
av>=12.0

tensorflow[and-cuda]>=2.16
scikit-learn>=1.4
scikit-image>=0.22
pandas>=2.1
pillow>=10.0
keras-vggface>=0.6

PySide6>=6.6
